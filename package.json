{
  "name": "portfolio-ecosystem",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=apps/portfolio && node -e \"const fs=require('fs'); fs.rmSync('./dist', {recursive:true, force:true}); fs.renameSync('./apps/portfolio/dist', './dist')\""
  }
}
